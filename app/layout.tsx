import type {ReactNode} from 'react';
export const metadata={title:'IP Protection Dashboard',description:'Manage visual IP blocking for your stores'};
export default function Root({children}:{children:ReactNode}){return <html lang="en"><body style={{margin:0,background:'#f6f8fa',fontFamily:'Arial,sans-serif'}}>{children}</body></html>}
